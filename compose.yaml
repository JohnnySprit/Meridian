services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=meridian'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=meridianAdmin'
    ports:
      - '52327:5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
